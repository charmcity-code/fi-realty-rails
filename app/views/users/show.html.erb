<h4>Realtor Details</h4>

Welcome <%= @user.username %>!

<h5>Your Upcoming Appointments:</h5>

<% @appointments.each do |a| %>
  <% if a.listing.user.id == current_user.id %>
    <ul>
      <li>
        <%= render partial: 'appointments/appointments', locals: {a: a} %>
  <% end %>
<% end %>

<br><%= link_to "Log Out", logout_path, method: :post %>