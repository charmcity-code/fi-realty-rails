<%= form_for @appointment do |f| %>
Select from existing buyer:
<%= f.collection_select :buyer_name, Buyer.all, :name, :name %><br>

Select listing:
<%= f.collection_select :listing_street, Listing.all, :street, :street%><br><br>

Appointment Date:
<%= f.text_field :date, placeholder: 'Use natural language'%><br>
<%= f.submit %>
<% end %>